<%- include('partials/header') %>

<h1><%= oneCard.name %></h1>

<div class="cardsContainer">
    <div class="card">
        <img src="/visuals/<%= oneCard.visual %>" alt="<%= oneCard.name %> illustration">
       <p class="cardName"><%= oneCard.name %></p>
      </a>
      <a class="link--addCard" title="Ajouter au deck" href="/deck/add/<%= oneCard._id %>">[ + ]</a>
    </div>

</div>

<%- include('partials/footer') %>